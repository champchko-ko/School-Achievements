import type { Metadata } from "next";
import { Tajawal } from "next/font/google"; // استخدام خط تجوال العربي العصري
import "./globals.css";
import Sidebar from '../components/Sidebar';

const tajawal = Tajawal({ subsets: ["arabic"], weight: ['400', '500', '700', '900'] });

export const metadata: Metadata = {
  title: "منصة إنجازات المدرسة",
  description: "تطبيق لتوثيق وتحفيز الإنجازات المدرسية",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="ar" dir="rtl">
      <body className={tajawal.className}>
        <div className="flex h-screen overflow-hidden bg-gray-100">
          
          <Sidebar />

          {/* منطقة المحتوى الرئيسية */}
          <main className="flex-1 overflow-y-auto p-4 md:p-8 print:p-0 print:overflow-visible">
            {children}
          </main>

        </div>
      </body>
    </html>
  );
}